# Base URL of your FastAPI backend once it exists, e.g. http://localhost:8000/api
VITE_API_BASE_URL=/api

# Set to "false" to switch every service in src/services from mock data
# to real fetch calls against VITE_API_BASE_URL.
VITE_USE_MOCK=true
